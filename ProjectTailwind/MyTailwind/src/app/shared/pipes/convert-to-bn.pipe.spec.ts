import { ConvertToBnPipe } from './convert-to-bn.pipe';

describe('ConvertToBnPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToBnPipe();
    expect(pipe).toBeTruthy();
  });
});
